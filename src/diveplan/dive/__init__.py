"""Dive layer: profile geometry, model runs, reports, and formatters.

Import the public names from the package root (``from diveplan import
DiveProfile, Dive, DiveReport``). ``diveplan.dive.dive_profile`` is the
documented home of the concrete validation-error subclasses.
"""
